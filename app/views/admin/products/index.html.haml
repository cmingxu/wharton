
%table.table
  %tr
    %th 型号
    %th 描述
    %th
    %th
    %th

  - @products.each do |product|
    %tr
      %td= product.product_model
      %td= product.description
      %td= link_to '编辑', edit_admin_product_path(product)
      %td= link_to '删除', admin_product_path(product), :confirm => 'Are you sure?', :method => :delete

%br

= link_to '新建', new_admin_product_path
